1) Handle authentication using local storage.
2) 2 pages - signup and profile.
On clicking signup, use local storage to create the state of the user - it should contain all the details taken in signup, plus should also consist of an access token - let this access token be a random 16-byte string generated by you randomly.
3)Once there is a state of the user in the local storage, redirect the user to a page called /profile. Display the user’s details in the respective fields in profile page.
4)On clicking the logout button, set the local storage back to null and also get rid of the access token.
5)Using scripts ensure that when a user manually tries to go to /profile, if the local storage does not contain the accesstoken the user is redirected to the signup page.

5)Similarly if the localstorage has an accesstoken and tries to go to the signup page, using scripts ensures that the user can't do so and is redirected to the profile page.
6)Show success message in green text, only if all mandatory fields for signup is provided correctly and then redirect to /profile.
Else show error message in red.